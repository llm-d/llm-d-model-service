apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llm-d-modelservice.fullname" . }}-test-connection"
  labels:
    {{- include "llm-d-modelservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llm-d-modelservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
