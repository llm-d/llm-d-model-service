{{- if and .Values.prefill .Values.multinode }}
apiVersion: leaderworkerset.x-k8s.io/v1
kind: LeaderWorkerSet
metadata:
  name: {{ include "llm-d-modelservice.fullname" . }}-prefill
  labels:
    {{- include "llm-d-modelservice.labels" . | nindent 4 }}
    {{- include "llm-d-modelservice.prefilllabels" . | nindent 4 }}
spec:
  {{- if not .Values.prefill.autoscaling.enabled }}
  replicas: {{ default 1 .Values.prefill.replicas }}
  {{- end }}
  leaderWorkerTemplate:
    size: {{ int (include "llm-d-modelservice.dataParallelism" .Values.prefill.parallelism) }}
    leaderTemplate:
      metadata:
        labels:
          {{- include "llm-d-modelservice.prefilllabels" . | nindent 10 }}
      spec:
        {{- (include "llm-d-modelservice.modelPod" .) | nindent 6 }}
        {{- with .Values.prefill.containers }}
        containers:
          {{- range . }}
          {{- (include "llm-d-modelservice.container" (dict "container" . "authSecretName" $.Values.modelArtifacts.authSecretName "parallelism" $.Values.prefill.parallelism)) | nindent 10 }}
          {{- end }}
        {{- end }}
      volumes:
      {{- with .Values.prefill.volumes }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- include "llm-d-modelservice.mountModelVolumeVolumes" . | nindent 10 }} 

    workerTemplate:
      metadata:
        labels:
          {{- include "llm-d-modelservice.prefilllabels" . | nindent 10 }}
      spec:
        {{- with .Values.prefill.containers }}
        containers:
          {{- range . }}
          {{- (include "llm-d-modelservice.container" (dict "container" . "authSecretName" $.Values.modelArtifacts.authSecretName "parallelism" $.Values.prefill.parallelism)) | nindent 10 }}
          {{- end }}
        {{- end }}
      volumes:
      {{- with .Values.prefill.volumes }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- include "llm-d-modelservice.mountModelVolumeVolumes" . | nindent 10 }} 

{{- end }}
