apiVersion: v1
kind: Service
metadata:
  name: {{ include "llm-d-modelservice.fullname" . }}
  labels:
    {{- include "llm-d-modelservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llm-d-modelservice.selectorLabels" . | nindent 4 }}
