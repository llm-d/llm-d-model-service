apiVersion: v1
kind: ConfigMap
metadata:
  name: basic-basic-conf
data:
  decodeDeployment: |
    spec:
      replicas: 2
      template:
        spec:
          containers:
          - name: llm
            command:
            - sleep
